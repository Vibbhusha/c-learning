#include<stdio.h>
int main() {
    int age=25;
    
    if(age>=18)
       printf("eligible to vote");
    else if(age<18)
       printf("not eligible to vote");
    else
       printf("zero");
    return 0;
}
